name: build-service CI

on:
  push:
    paths:
      - "build-service/**"
      - ".github/workflows/build-service.yml"
  pull_request:
    paths:
      - "build-service/**"

jobs:
  ci:
    name: Lint, Vet & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: build-service

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache-dependency-path: build-service/go.sum

      - name: Download dependencies
        run: go mod download

      - name: go vet (static analysis)
        run: go vet ./...

      - name: Build binary
        run: go build -o /dev/null ./...
